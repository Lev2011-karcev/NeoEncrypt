#include <cstdint>
#include <vector>
#include <algorithm>
#include <stdexcept>

using namespace std;

/* ===================== S-BOX ===================== */
static const uint8_t sbox[256] = {
    
};

/* ===================== RCON ===================== */
static const uint8_t Rcon[11] = {
    0x00,0x01,0x02,0x04,0x08,0x10,0x20,0x40,0x80,0x1B,0x36
};

/* ===================== GF(2^8) ===================== */
uint8_t xtime(uint8_t a) {
    return (a & 0x80) ? ((a << 1) ^ 0x1b) : (a << 1);
}
uint8_t mul3(uint8_t a) {
    return xtime(a) ^ a;
}

/* ===================== AES STEPS ===================== */
void SubBytes(vector<vector<uint8_t>>& state) {
    for (int i = 0; i < 4; i++)
        for (int j = 0; j < 4; j++)
            state[i][j] = sbox[state[i][j]];
}

void ShiftRows(vector<vector<uint8_t>>& state) {
    rotate(state[1].begin(), state[1].begin() + 1, state[1].end());
    rotate(state[2].begin(), state[2].begin() + 2, state[2].end());
    rotate(state[3].begin(), state[3].begin() + 3, state[3].end());
}

void MixColumns(vector<vector<uint8_t>>& state) {
    for (int c = 0; c < 4; c++) {
        uint8_t a = state[0][c], b = state[1][c];
        uint8_t c1 = state[2][c], d = state[3][c];
        state[0][c] = xtime(a) ^ mul3(b) ^ c1 ^ d;
        state[1][c] = a ^ xtime(b) ^ mul3(c1) ^ d;
        state[2][c] = a ^ b ^ xtime(c1) ^ mul3(d);
        state[3][c] = mul3(a) ^ b ^ c1 ^ xtime(d);
    }
}

void AddRoundKey(vector<vector<uint8_t>>& state, const uint8_t* rk) {
    for (int i = 0; i < 4; i++)
        for (int j = 0; j < 4; j++)
            state[i][j] ^= rk[i * 4 + j];
}

/* ===================== KEY EXPANSION ===================== */
void RotWord(uint8_t* w) {
    uint8_t t = w[0];
    w[0] = w[1];
    w[1] = w[2];
    w[2] = w[3];
    w[3] = t;
}

void SubWord(uint8_t* w) {
    for (int i = 0; i < 4; i++)
        w[i] = sbox[w[i]];
}

vector<uint8_t> KeyExpansion(const uint8_t key[16]) {
    vector<uint8_t> rk(176);
    for (int i = 0; i < 16; i++) rk[i] = key[i];

    int bytes = 16, rcon = 1;
    uint8_t temp[4];

    while (bytes < 176) {
        for (int i = 0; i < 4; i++)
            temp[i] = rk[bytes - 4 + i];

        if (bytes % 16 == 0) {
            RotWord(temp);
            SubWord(temp);
            temp[0] ^= Rcon[rcon++];
        }

        for (int i = 0; i < 4; i++) {
            rk[bytes] = rk[bytes - 16] ^ temp[i];
            bytes++;
        }
    }
    return rk;
}

/* ===================== AES ENCRYPT ===================== */
vector<uint8_t> aes_encrypt(const uint8_t input[16], const uint8_t key[16]) {

    vector<vector<uint8_t>> state(4, vector<uint8_t>(4));
    for (int i = 0; i < 16; i++)
        state[i % 4][i / 4] = input[i];
    vector<uint8_t> rk = KeyExpansion(key);

    AddRoundKey(state, &rk[0]);

    for (int r = 1; r <= 9; r++) {
        SubBytes(state);
        ShiftRows(state);
        MixColumns(state);
        AddRoundKey(state, &rk[r * 16]);
    }

    SubBytes(state);
    ShiftRows(state);
    AddRoundKey(state, &rk[160]);

    vector<uint8_t> out(16);
    for (int i = 0; i < 16; i++)
        out[i] = state[i % 4][i / 4];

    return out;
}
